<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <title><%= titlu || "Eroare server" %></title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; margin-top: 10vh; }
        h1 { font-size: 3rem; color: #DB372A; }
        p { font-size: 1.5rem; }
        .msg { color: #555; margin-top: 2rem; }
    </style>
</head>
<body>
    <h1>Eroare</h1>
    <% if (imagine) { %>
        <img src="<%= imagine %>" alt="Eroare" style="max-width: 300px; width: 100%; height: auto; display: block; margin: 2rem auto;">
    <% } %>
    <p><%= titlu || "A apărut o eroare pe server." %></p>   
<% if (text) { %>
    <div class="msg"><%= text %></div>
<% } %>

    <a href="/">Înapoi la pagina principală</a>
</body>
</html>